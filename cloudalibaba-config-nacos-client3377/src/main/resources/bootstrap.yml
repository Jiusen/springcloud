server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: 39.97.233.41:8848 #服务注册中心地址
      config:
        server-addr: 39.97.233.41:8848 #配置中心地址
        file-extension: yaml #指定yaml格式的配置
        group: TEST_GROUP


  #${prefix}-${spring.profiles.active}.${file-extension}
  #nacos-config-client-test.yaml


